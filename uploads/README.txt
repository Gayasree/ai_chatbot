# Uploads Folder

This folder stores uploaded PDF files temporarily during the session.

Files are automatically saved here when students upload PDFs through the web interface.

Note: For production deployment, consider using cloud storage (AWS S3, Azure Blob) instead of local file storage.
